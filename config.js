// GitHub API Configuration
const GITHUB_CONFIG = {
    owner: 'vophamthanhan',
    repo: 'hagiang',
    branch: 'main',
    filePath: 'data.js',
    // ⚠️ QUAN TRỌNG: Thay YOUR_GITHUB_TOKEN bằng token thật của bạn
    // Token lấy từ: https://github.com/settings/tokens/new
    // Quyền cần có: repo (full control)
    token: ''
};

// ⚠️ LƯU Ý BẢO MẬT:
// Trong production, token nên được lưu ở backend hoặc sử dụng GitHub OAuth
// Hiện tại để đơn giản, token được lưu ở đây
// Chỉ chia sẻ token với người bạn tin tưởng!
